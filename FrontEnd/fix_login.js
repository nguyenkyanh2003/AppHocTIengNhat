// HƯỚNG DẪN FIX LỖI ĐĂNG NHẬP
// Copy paste các lệnh này vào Chrome DevTools Console (F12)

// Bước 1: Clear tất cả dữ liệu cũ
localStorage.clear();
sessionStorage.clear();
console.log('✅ Đã xóa dữ liệu cũ!');

// Bước 2: Refresh trang
console.log('📝 Bây giờ refresh trang (F5) và đăng nhập lại!');
